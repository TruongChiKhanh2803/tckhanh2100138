<table>
    <thead>
        <tr>
            <th>STT</th>
            <th>Username</th>
            <th>Full name</th>
            <th>Address</th>
            <th>Setting</th>
        </tr>
    </thead>
    <tbody>
        <% users.forEach((user, index)=> { %>
            <tr>
                <td>
                    <%= index + 1 %>
                </td>
                <td>
                    <%= user.username %>
                </td>
                <td>
                    <%= user.fullname %>
                </td>
                <td>
                    <%= user.address %>
                </td>
                <td>
                    <a href="/users/<%= user.id %>">👁</a>
                    <a href="/users/<%= user.id %>/edit">✏️</a>
                    <form action="/users/<%= user.id %>/delete" method="POST" style="display:inline;">
                        <button type="submit">🗑️</button>
                    </form>
                </td>
            </tr>
            <% }) %>
    </tbody>
</table>